[project]
name = "ledgerdb-api"
version = "0.1.0"
description = "Backend API for LedgerDB."
requires-python = ">=3.13"
dependencies = [
    "django>=6.0.7",
    "djangorestframework>=3.17.1",
    "psycopg[binary]>=3.3.4",
]
